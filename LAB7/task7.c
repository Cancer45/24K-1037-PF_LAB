#include <stdio.h>
 
int main() {
    
    //init array
    char input[100];
    int result = 0;
    
    //take input
    printf("input expression to be computed (only use '+' and '-' operators): ");
    scanf("%s", &input);
    
    //calculate edpression
    for (int i = 0; input[i] != '\0'; i++)
    {
        
    }

    return 0;
}
